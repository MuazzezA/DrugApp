import { Dimensions } from 'react-native';
const { width, height } = Dimensions.get('window');

export default {
    base: 8,
    font: 14,
    radius: 15,
    padding: 25,
    icon: 25,
    WIDTH: width,
    HEIGHT: height,
    componentHeight: 45,

}